# Artefactos de LaTeX
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk

# Temporales
*.tmp
compile*.log

# Vercel
.vercel

.vercel
.env*

# Fuera del repositorio publico, por ahora.
# El guion cita un proyecto de cliente (Simply Pool, colaboracion con Joel), con
# el detalle de un incidente interno, ademas de rutas del equipo y cifras aun sin
# verificar. Entra cuando se decida que hacer con eso.
guion/
REGLAS.md
